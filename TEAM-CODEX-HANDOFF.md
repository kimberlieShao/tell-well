# Pulsewise：两人协作与 Codex 任务说明

> 最新范围：仅支持用户本人记录。已取消家庭成员、代记录和多成员档案；请以此为准。


这份文件是 Kimberly 的中文工作说明，不表示下列新接口已经实现。现在只有 Kimberly 和队友两人负责这块。Kimberly 把下方 A 部分复制给自己的 Codex；队友使用独立英文文件 TEAMMATE-CODEX-TASK.md。无需等 Gemini 修好才开始前端工作。

英文版文件：[TEAMMATE-CODEX-TASK.md](TEAMMATE-CODEX-TASK.md)。下方 B 部分保留为中文参考，方便 Kimberly 理解队友的任务。

## 先发给队友的话

我准备做首次使用设置和用户档案，包括称呼、健康背景、当前用药、饮食习惯，以及想记录的内容。你先负责排查 Gemini 的 limit 报错，并确认登录和档案存储怎么接。我们先不同时改现有语音流程和 index.html。我先用 mock 数据做独立页面，你把最新集成版本、接口文档和确定好的请求/响应格式发我，最后再合并接入。

## 分工

| 工作 | 负责人 |
| --- | --- |
| Gemini 报错、请求次数和重试排查 | 队友的 Codex |
| 后端认证、用户档案存储、记录归属 | 队友的 Codex |
| 首次使用设置、档案编辑、登录页面 | Kimberly 的 Codex |
| 现有语音/API 集成、主入口最终接线 | 队友的 Codex，前端页面准备好后统一接入 |
| 联调、确认数据隔离和原有流程未损坏 | 两人共同 |

## A. Kimberly 复制给自己的 Codex

请为当前 Pulsewise 项目增量开发首次使用设置和用户档案前端。

### 开工前

1. 先检查 git 分支、未提交变更和当前代码结构。保护已有修改，不覆盖、不 reset、不强制 push。
2. 当前本地可能只有旧版 index.html，队友已经有新的后端集成版本。先确认基线；拿不到最新版本时，只做可独立预览的前端模块，不覆盖集成入口。
3. 在独立分支工作，建议 codex/patient-onboarding。优先新增独立 HTML/CSS/JS 文件，并限制 CSS 作用范围；如果项目已有组件结构，则遵循现有结构。
4. 不修改 Home、Meals、Trends、More、Daily Check-in、SpeechRecognition、症状/药物解析或后端代码。此次先完成独立模块，接入现有导航另行联调。

### 做什么

沿用森林绿、浅绿色、白色圆角卡片、Georgia 标题，使用大字体和大按钮。

新增首次使用流程，每步简短，可返回，可跳过非必填健康信息：
- 基本信息：称呼、年龄段。
- 健康背景：用户自述基础疾病，多选、其他、没有、不愿透露。不要自动诊断。
- 当前用药：名称、剂量、服用时间，支持新增、编辑、删除和跳过。
- 饮食偏好：饮食习惯；过敏信息单独填写，不与饮食偏好混为一谈。
- 记录偏好：症状、药物、饮食、水、咖啡因、血压、血糖等，可多选。
- 使用偏好：字体大小、偏好语音。
- 确认页面：展示用户实际填写的资料，可返回修改。

不要把老年人、免疫系统疾病患者等做成互斥身份。年龄、基础疾病、使用偏好分别存储，允许重叠。

新增可独立使用的档案编辑模块。所有表单读写统一 userProfile，并保留“未填写”和“明确选择没有”的区别。

登录页面必须等待认证约定后接线。可以先做明确标注的演示入口，但不要把前端切换姓名或 localStorage 当作真实登录，不在前端存储密码。

将数据访问封装为独立适配层，例如 loadProfile() / saveProfile(profile)。默认使用明确标注的 mock 实现；后端接口确认后再替换。首次设置本身不应依赖 Gemini。

### 验证与交付

验证前进/返回不丢数据、跳过不生成假数据、药物增删改、确认后重新编辑、键盘操作和窄屏显示。只声称实际完成的测试。

交付：改动文件、独立预览方式、mock/真实功能边界、需要后端提供的字段与接口。暂不推送 main，不替换队友的 index.html。

## B. 队友复制给自己的 Codex

请先排查 Pulsewise 的 Gemini limit 报错，再整理认证与用户档案接口。Kimberly 正在独立开发 onboarding/profile 前端，请不要同时重写这些页面。

### 第一阶段：定位 Gemini 错误

1. 先检查当前分支和未提交变更，保留所有已有工作。在独立分支处理，建议 codex/gemini-profile-api。
2. 从一次实际失败请求收集：HTTP 状态码、Gemini 原始错误体、错误类型/详细信息、请求模型、请求时间和后端请求 ID。禁止输出完整 API key、Authorization header 或敏感健康原文。
3. 检查前端的 quota 提示是否来自统一 catch 映射。不要先假设截图文案等于实际错误原因。
4. 核对实际运行进程加载的配置来源、key 对应项目、模型、API 服务与查看配额的项目是否一致。不要把用户认为配额正常当作排查结论，也不要未经证据断言额度耗尽。
5. 使用同一后端配置做一次最小请求；与页面请求比较，区分服务端拒绝和应用重复调用。
6. 检查连续语音是否在 interim/final/onend 等多处重复提交、是否并发请求、是否有无限或过密重试。避免为排查制造更多请求。
7. 如需查 SDK/模型/配额规则，使用当前官方文档。根据实际错误分别处理认证、模型、配额、限流、网络等问题；只对适合重试的错误做有界退避，遵循 Retry-After（如果有）。
8. 不通过切换账号/key 绕过限制，不擅自开启付费或调整预算。不修改 SpeechRecognition 来掩盖后端错误。
9. 修复后验证失败不会覆盖已保存记录，UI 提示对应真实原因。明确报告根因、证据、修改、测试，以及仍需用户在控制台完成的操作。

### 第二阶段：确认认证和档案能力

检查当前项目实际已有的认证、会话、数据库和用户模型，优先复用。不要假设 check-in session 等于登录会话。

先更新接口文档并给出可复制的请求/响应示例：
- 登录、注册、退出，以及前端如何判断登录状态。
- 当前用户档案的读取/更新。
- onboardingCompleted 如何保存和读取。
- Check-in、Meals 等记录如何归属用户。
- 数据是持久化，还是重启/超时就丢失的演示内存数据。

如果已有持久化和认证方案，在现有架构内补齐接口。如果没有，先给出需要双方确认的最小方案和外部服务配置，不自行购买服务；不要实现“看似登录成功、实际没有账户隔离”的假认证。

后端必须根据认证会话确定用户身份，不信任前端传入的任意 userId。验证不同用户不能读取或覆盖彼此档案及记录。不要把服务器 API key 发到浏览器。

分析接口未来可以读取档案中的用药和记录偏好，但这不是改写当前语音流程的授权。先提供档案能力，后续单独接入个性化。用户档案不能被当成当天实际报告或测量结果。

### 交付给 Kimberly

- 可安全合并的分支/commit，以及基线说明。
- 最新 FRONTEND-INTEGRATION.md 和 backend/API.md（或对应文档）。
- 认证方式、请求/响应示例、错误格式、持久化说明。
- 本地启动与测试命令、必要环境变量名称，不包含秘密值。
- Gemini 修复状态和未解决事项。

不要强推 main，不覆盖 Kimberly 的独立前端模块。

## 共同确认的数据草案

以下是讨论起点，不是已存在接口。没有填过的信息使用 null；用户明确选择“没有”才用空数组。药物 id 由实际实现生成。

```js
const userProfile = {
  displayName: "",
  ageRange: null,
  conditions: null,     // 用户自述，不是系统诊断
  medications: null,    // [{ id, name, dose, schedule }]
  dietaryPreferences: null,
  allergies: null,
  trackingPreferences: [],
  accessibility: {
    textSize: "normal",
    preferVoice: false
  },
  onboardingCompleted: false
};
```

建议讨论 GET /api/me、GET /api/me/profile、PATCH /api/me/profile；最终以现有后端结构和双方确认的文档为准。明确 PATCH 的 null/空数组语义、字段校验、认证失败响应和错误体格式。

userProfile 是长期档案；checkinState 是当次记录。两者不能混用。记录偏好只是用户想追踪什么，不代表已经有读数、服过药或出现了症状。

## 合并顺序

1. 队友保留并提交当前可工作的集成基线，提供给 Kimberly；不要传含 .env/key 的文件。
2. Kimberly 在独立模块中做前端，队友同时排查 Gemini、明确接口。
3. 双方确认字段与认证约定，前端切换适配层。
4. 队友统一把新入口接到集成版本：未登录 → 登录；未完成设置 → onboarding；已完成 → 现有 Home。
5. 联调：新用户、返回用户、跳过设置、编辑档案、退出/换账号、不同用户隔离、API 失败，以及原有 Daily Check-in/Meals/Trends/More。
6. 查看 diff 后合并；不要直接用旧版完整 index.html 覆盖新版。

首次设置和档案读写应该在 Gemini 不可用时仍能工作。前端开发不需要被 Gemini 问题阻塞。
