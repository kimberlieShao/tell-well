# API contract — version 1.0

Base URL during local development: `http://127.0.0.1:3001`.
Use `Content-Type: application/json` on every POST. All successful responses have the same keys, validated by `contracts/response.schema.json`. All four categories are always arrays. The only API endpoints are `POST /api/analyze` and `POST /api/checkin/save`.

## 1. Start a check-in

```js
const response = await fetch('http://127.0.0.1:3001/api/analyze', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    transcript: 'My knees hurt more today and I forgot my prednisone this morning.'
  })
});
const state = await response.json();
if (!response.ok) throw new Error(state.error.message);
```

Illustrative response (IDs/timestamps are generated by the backend):

```json
{
  "schemaVersion": "1.0",
  "sessionId": "11111111-1111-4111-8111-111111111111",
  "version": 1,
  "status": "collecting",
  "extractionMode": "demo",
  "symptoms": [{
    "id": "22222222-2222-4222-8222-222222222222",
    "name": "knee pain",
    "location": "knees",
    "severity": null,
    "severityScore": null,
    "trend": "worse",
    "functionalImpact": null,
    "duration": null
  }],
  "medications": [{
    "id": "33333333-3333-4333-8333-333333333333",
    "name": "Prednisone",
    "description": null,
    "dose": null,
    "status": "missed",
    "time": "morning"
  }],
  "diet": [],
  "vitals": [],
  "missingFields": ["symptoms[0].severity", "symptoms[0].functionalImpact"],
  "skippedFields": [],
  "needsClarification": false,
  "nextQuestion": {
    "id": "22222222-2222-4222-8222-222222222222:severity",
    "category": "symptoms",
    "entityId": "22222222-2222-4222-8222-222222222222",
    "field": "severity",
    "text": "Would you describe your knee pain as mild, moderate, or severe?",
    "type": "single_choice",
    "options": ["Mild", "Moderate", "Severe"]
  },
  "notices": ["Demo extraction uses limited phrase rules, not AI. It can miss details. Use fictional data and review every field."],
  "storage": "memory",
  "expiresAt": "2026-09-18T20:00:00.000Z",
  "savedAt": null
}
```

Unlike the initial planning example, “knees” is already known, so the backend does not ask for location. Field paths include the array index so the frontend knows which symptom is incomplete. Use stable item IDs for rendering keys and question IDs for answers; array indexes are only display paths.

## 2. Answer, skip, add information, or finish

Use the **latest** response in place of `state` and replace it with every new response. Send one action per request.

Button/form answer:

```js
const body = {
  sessionId: state.sessionId,
  version: state.version,
  answer: { questionId: state.nextQuestion.id, value: 'Moderate' }
};
```

Voice answer to the current question:

```js
const body = {
  sessionId: state.sessionId,
  version: state.version,
  questionId: state.nextQuestion.id,
  transcript: 'Moderate'
};
```

To add an unrelated new observation, send `{ sessionId, version, transcript }` **without** `questionId`. The backend does not assume an untagged short answer belongs to the current question.

Skip a question:

```js
const body = {
  sessionId: state.sessionId,
  version: state.version,
  action: 'skip',
  questionId: state.nextQuestion.id
};
```

“I'm not sure,” “I don't know” and “prefer not to say” as targeted answers also skip. Skipping leaves the field `null` and includes it in both `missingFields` and `skippedFields`.

Finish early and show review:

```js
const body = { sessionId: state.sessionId, version: state.version, action: 'review' };
```

`action: 'resume'` returns to remaining questions. Already skipped questions stay skipped; the user can fill those details at review. When there are no unskipped questions, status becomes `review` automatically. `nextQuestion` is `null` in review and saved states. **That is not proof the record is complete or medically safe.**

The unnamed-pill case produces `name: null`, `needsClarification: true`, and a text question with `options: []`. Show a text/voice input and Skip button; no medication list is invented. If the user identifies it, the backend updates the existing medication entry.

A typed medication-name guess such as “Maybe prednisone” returns `422 INVALID_ANSWER` without changing the record. The user can provide a clear name, skip, or say “I'm not sure.”

## 3. Save after the user confirms

POST `/api/checkin/save`:

```js
const body = {
  sessionId: state.sessionId,
  version: state.version,
  confirmed: true
};
```

An optional `record` contains **all four complete arrays**, after the user's edits:

```js
const body = {
  sessionId: state.sessionId,
  version: state.version,
  confirmed: true,
  record: {
    symptoms: editedSymptoms,
    medications: editedMedications,
    diet: editedDiet,
    vitals: editedVitals
  }
};
```

Keep existing item UUIDs when editing. Generate a unique UUID for any newly added item. Every object must include all its schema fields, with `null` for unknown values. You may remove mistakenly extracted items from the arrays or clear fields to `null`. See `contracts/record.schema.json`.

The response has `status: "saved"`, `savedAt`, a new version, and the saved arrays. It does not add a separate `success` key: check the HTTP status and `status`. The response still reports missing details. A completely empty record cannot be saved. Saved sessions cannot be modified. An exact repeated save request returns the same saved response.

## Record fields

| Category | Fields on each item, in addition to UUID `id` |
| --- | --- |
| symptoms | `name`, `location`, `severity` (mild/moderate/severe/null), `severityScore` (0–10/null), `trend` (better/same/worse/null), `functionalImpact`, `duration` |
| medications | `name`, `description`, `dose`, `status` (taken/missed/stopped/mentioned/null), `time` |
| diet | `description`, `time` |
| vitals | `name`, `value`, `unit`, `time` |

Names are required for symptoms/vitals; diet description is required. Other text fields are nullable. Vital values are strings to preserve readings such as `120/80`. Units and doses are never assumed. This API does not classify any reading as healthy or dangerous. Numeric severity is not converted into mild/moderate/severe.

## Errors

Errors use `{ "error": { "code": "...", "message": "...", "details": [] } }`.

| HTTP status | Meaning |
| --- | --- |
| 400 | Invalid JSON, missing fields, wrong schema, or missing confirmation |
| 403 | Browser origin not in backend CORS settings |
| 404 | Session absent/expired, server restarted, or wrong endpoint |
| 409 | Outdated version/question, overlapping request, already saved, or review required |
| 413 / 415 | Body too large / wrong Content-Type |
| 422 | Invalid answer, duplicate item IDs, or empty save |
| 502 | AI service failed, invalid AI output, or unknown entity reference; session unchanged |
| 503 | Demo session capacity reached |

On failed requests, retain the last successful response. A `409` means stop submitting against stale state. Keep one request in flight per session. This MVP has no session-retrieval endpoint: if a successful analyze response is lost over the network, start a new check-in from the transcript rather than retrying indefinitely with a stale version.
